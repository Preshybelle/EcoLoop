# Backend scan API base URL (no trailing slash).
# Replace with the URL from the backend team when ready.
# Example: https://api.ecoloop.com  or  http://localhost:8000
VITE_SCAN_API_URL=http://localhost:8000
